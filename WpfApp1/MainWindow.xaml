<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:vm="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
            <StackPanel>
                <Button Content="Add a layer" Command="{Binding LoadShapeCommand}" Margin="5" Height="40"/>
                <ListBox Margin="5" ItemsSource="{Binding ShapeFiles}"
                 SelectedItem="{Binding SelectedShape}"
                 DisplayMemberPath="Name"/>
                <Button Content="Recenter" Command="{Binding RecenterMapCommand}" Margin="5" Height="40"/>
            </StackPanel>
        </Border>
        <Border Grid.Column="1" Margin="5" BorderBrush="Gray" BorderThickness="1">
            <Grid Name="MapControl" >
                
            </Grid>
        </Border>
    </Grid>
</Window>